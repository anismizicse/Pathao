<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ourdreamit.pathao">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.NavigationDrawerActivity"
            android:theme="@style/AppTheme.NoActionbar"/>
        <activity android:name=".activities.AppResources" />
        <activity android:name=".activities.LayoutBasics" />
        <activity android:name=".activities.FeatureActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.OrderFood" />
        <activity android:name=".activities.HomeScreen" />
        <activity android:name=".activities.ActivityA" />
        <activity android:name=".activities.ActivityB" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.InternetDetector" />

        <service android:name=".services.CountingService" />
    </application>

</manifest>